<form method="post" action="{{ path('categorie_eleve_delete', {'id': categorie_eleve.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette catégorie d'élève ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_eleve.id) }}">
    <button class="btn btn-danger" id="btnSuppr">Supprimer</button>
</form>

