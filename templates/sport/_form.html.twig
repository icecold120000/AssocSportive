	{{form_start(form)}}
		<div>
			{{form_label(form.nomSport)}}
		</div>
        <div id="divInput">
            {{form_widget(form.nomSport)}}
        </div>
        <div class="erreur">
            {{form_errors(form.nomSport)}}
        </div>
        <div>
            <div id="divAjout" class="form-actions">
                <button type="submit" class="btn btn-success">
                    Enregistrer le sport
                </button>
            </div>
        </div>
    {{form_end(form)}}