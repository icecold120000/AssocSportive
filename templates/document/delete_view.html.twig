{% extends 'base.html.twig' %}

{% block title %}Document{% endblock %}

{% block body %}
    <h1>Suppression d'un document</h1>

	<p>Cliquez sur le bouton Supprimer pour supprimer le document choisi : {{document.nomDocument}}</p>


    {{ include('document/_delete_form.html.twig') }}

    <div>
        <a href="{{ path('document_admin') }}">
            <button class="btn btn-secondary" id="btnRetour">
                Revenir dans la liste
            </button>
       </a>
    </div>
{% endblock %}